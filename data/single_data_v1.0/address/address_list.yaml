#地址地址列表接口 版本：single_data_v1.0 用例编号test_address_list
- body:
    page: '1'
    size: '2'
    key: 江
  severity: normal #用例等级
  story: 地址列表 #用例功能
  feature: 地址模块 #用例模块
  case_name: test_address_list_地址列表请求成功
  url: /address/list
  schema:
    type: object
    required: [ ]
    properties:
      msg:
        type: string
        pattern: ^success
      code:
        type: integer
        maximum: 1
      data:
        type: object
        required: [ ]
        properties:
          total:
            type: integer
          page:
            type: number
          pages:
            type: integer
          size:
            type: number
          key:
            type: string
          filter_pojos:
            type: array
            items:
              type: string
          order_by_pojos:
            type: array
            items:
              type: string
          address_vos:
            type: array
            items:
              type: string
